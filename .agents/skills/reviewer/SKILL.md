---
name: reviewer
description: "ローカルの未コミット差分、指定ファイル、コミット範囲、ブランチ差分を、複数の独立したレビュー担当に並列確認させて統合するローカル専用レビュー。自然言語トリガー例: 「未コミット差分をレビューして」／「このファイルをレビューして」／「このコミット範囲をレビューして」／「このブランチ差分をレビューして」。該当する依頼ではスキル名がなくても使い、ユーザーが /reviewer と入力したら必ず使う。PR URLやremote branch固有のレビューは /review-pr に委ねる。"
---

# Reviewer

ローカルのコード変更を複数観点で読み取り専用レビューし、検証済みの指摘を1つの結果に統合する。

## 入力を解釈する

`$reviewer` に続くテキストをレビュー対象として扱う。次のオプションを認識する。

- `--all-reviewers`: 利用可能な担当をすべて使う。
- `--reviewers=<roles>`: カンマ区切りで担当を明示する。使用可能な値は `correctness`, `consistency`, `quality`, `security`, `architecture`。

オプション以外の対象は次の順序で解釈する。

1. ファイルまたはディレクトリなら、そのパスに限定する。
2. `..` を含むコミット範囲なら、その範囲を使う。
3. 解決可能なブランチまたはコミットなら、マージベースから `HEAD` までを対象にする。
4. 指定がなければ、追跡済み・未追跡を含むローカルの未コミット変更を対象にする。

まず `git status --short` と適切な `git diff` / `git diff --cached` / `git merge-base` を使って対象を確定する。未追跡ファイルは個別に読む。変更がなければ、その旨を伝えて終了する。

## 担当を選ぶ

明示指定があればその担当を使う。`--all-reviewers` なら利用可能な全観点を使う。未指定時は変更内容と、失敗時に起きる具体的な実害から必要な観点だけを選ぶ。レビュー不要な局所変更は親の diff 照合と focused check で足りる場合がある。

- `correctness` は挙動、要件、データ整合性、競合、回帰に影響する変更で選ぶ。
- `consistency` は既存機能の拡張や既存パターンへの追加で選ぶ。近傍コード、命名、エラー処理、テスト慣習との不一致を調べる。
- `quality` は新しい関数・型・分岐、または意味のあるロジック変更で選ぶ。複雑さ、重複、保守性、テスト可能性を調べる。
- `security` は認証・認可、入力、SQL、秘密情報、暗号、ファイル、ネットワーク、並行処理、権限に触れる変更で選ぶ。悪用可能な経路と防御不足を調べる。
- `architecture` は新規ファイル、複数レイヤー、API・DBスキーマ・依存関係・責務境界に触れる変更で選ぶ。結合、境界、移行安全性を調べる。

## レビューを実行する

選んだ担当ごとに、現在のランタイムが提供する委譲 primitive を使う。独立した観点は並列化できるが、容量不足なら wave を分けるか親が直接確認する。固定人数、起動前宣言、同時性、特定のレポート形式を品質条件にしない。同じ差分を渡し、各依頼に次の条件を含める。

- 担当名と上記の担当固有観点。
- 対象のパス、範囲、または差分の特定方法。
- 差分だけでなく、関連する周辺実装・呼び出し元・テストも読む。
- レビュー対象のファイルを変更しない。コミット、フォーマット、生成処理も行わない。
- スタイル上の好みではなく、変更によって生じる具体的で再現可能な問題を優先する。
- 各指摘を `P0`〜`P3`、タイトル、根拠、影響、絶対パスと行番号、短い修正方針で返す。
- 問題がなければ `No findings` と明記する。

利用可能な委譲機能を使い、担当のモデルや実行方式はランタイムへ委ねる。担当が別担当を起動せず、親の計画・scope・受入を変更しないよう境界を渡す。

## 結果を統合する

起動した担当の結果を親が実コード、仕様、再現、テストに照らして再検証する。誤検知を除き、同じ原因の指摘を統合し、重要度順に並べる。未起動の担当の verdict や未生成の成果物を補完しない。

重大度は次の基準を使う。

- `P0`: 広範な停止、不可逆なデータ損失、重大な侵害など、直ちに対応が必要。
- `P1`: 通常利用で起き得る重大な誤動作、セキュリティ問題、移行事故。
- `P2`: 条件付きの不具合、限定的な回帰、保守上の実害。
- `P3`: 小さな不具合または低リスクの改善。

最終回答は次の順序にする。

1. `VERDICT: FAIL`（検証済みの `P0` または `P1` がある場合）または `VERDICT: PASS`。
2. `Findings`。各指摘は重要度、簡潔な見出し、根拠、影響、クリック可能な絶対ファイルリンクを含める。
3. `Reviewer coverage`。実行した担当と、問題なしだった担当を簡潔に示す。
4. 必要な場合だけ、前提や未検証事項を示す。

指摘がなければ `No findings.` と明記する。レビュー依頼だけではファイルへレポートを書き込まない。ユーザーが保存を明示した場合に限り、指定先または `docs/reviews/` に保存する。
