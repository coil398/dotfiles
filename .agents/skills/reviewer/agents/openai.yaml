interface:
  display_name: "Reviewer"
  short_description: "差分・ファイル・コミット範囲・ブランチを必要な観点からレビューする"
  default_prompt: "Use $reviewer to review the requested diff, files, commit range, or branch, choose the required perspectives, and summarize the result."

policy:
  allow_implicit_invocation: true
