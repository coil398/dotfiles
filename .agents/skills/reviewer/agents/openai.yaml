interface:
  display_name: "Reviewer"
  short_description: "ローカルのコード差分を必要な観点からレビューする"
  default_prompt: "Use $reviewer to review the local diff, choose the required perspectives, and summarize the result."

policy:
  allow_implicit_invocation: true
