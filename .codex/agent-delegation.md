<!-- Codex-native editable overlay. Maintained directly; sync preserves this file. -->

# コードベース探索の委譲 — 詳細手順

親 Astra は探索と結果統合を所有する。小さい調査や全体設計と密接な探索は直接行い、独立した調査領域は具体的な問いと範囲を定めて標準子へ委譲する。この文書は探索を委譲するときのruntime手順であり、すべての作業での事前読込は要求しない。専門手順の原本は `.agents/skills/research/references/explorer.md` と `tech-validator.md`、読者の分担は共有 `AGENTS.md` の「作業の配分とSkill」に従う。

## このルールの適用範囲（「オーケストレーター」とはこれら全てを指す）

- スキル経由で稼働中のメイン Codex（native Skills・プラグイン提供のスキルを含む）
- ユーザーから直接依頼を受けたメイン Codex のうち、複数ファイルにまたがる調査・設計相談・レビュー準備など探索が発生する場面

子からの追加委譲は、親が明示的に認めた場合だけ行う。利用可能な collaboration API と実効の深さ・同時実行上限に従い、API がない場合は勝手に別の起動経路へ迂回せず親へ必要な調査を返す。実装・レビュー・テストの配分、ユーザー対話、完了判定は親に集約する。

## 直接確認に適する例

- 単一ファイルのピンポイント確認（パスが既知・対象は1ファイル・広域パターン探索を兼ねない）は直接 Read してよい
- ユーザーから特定ファイルを提示された場合、その該当ファイルの Read
- git status / git log / git diff など、VCS 由来の軽量な状態確認

## 独立した調査として委譲できる例

- 「このプロジェクトの○○パターンを調べる」→ Grep/Glob 相当の作業
- 複数ファイルの連続 Read による構造把握
- ディレクトリ階層の列挙、同一レイヤーの既存コンポーネント洗い出し
- ライブラリ公式 doc の WebFetch/WebSearch による裏取り

## 運用ルール

- 必要な初回探索を行う。小さく不可分なタスクには explorer を人工的に追加しない
- 独立した調査領域は並列起動する。並列数はアクティブ設定の `max_concurrent_threads_per_session` と実行時に利用可能な空き枠の低い方を上限とし、設定値を埋めることは要求しない。完了済みを空き枠と推測せず、利用可能な既存 thread は `followup_task` で再利用する。以下の判断軸で領域を分割すること:
  - 各領域が本当に独立か（重複が大きいなら 1 エージェントに統合する）
  - 各領域に十分な調査量があるか（薄い領域は他とまとめる）
  - 依存がある領域は先行結果を受け取ってから開始する
- 各エージェントには調査対象と期待する出力を具体的に指示する
- 呼び出し元は返ってきた探索レポートをもとに判断し、必要な実作業を担当エージェントへ委譲し、結果を回答と最終成果へ統合する
- 初回レポートで完了判断を妨げる不明点があれば、その問いに限定して追加調査する。同じ未解決原因での盲目的な再試行や、完了条件に関係しない際限のない探索は行わない
- 既存コード・API仕様の調査とライブラリ選定では、必要な専門referenceの実体パスを子へ渡し、子自身にReadさせる。親は委任のために専門本文を先読みしてプロンプトへ転記しない。親が直接探索する場合は該当referenceを読み、統合時は根拠の照合に必要な部分を読む
- 標準子のモデル選択は `.codex/codex-native-supplement.md` と実効configに従う。子には対象版・専門原本の実体path・調査範囲・未確認の返却を指定する
- 調査担当は結果を親へ返し、reportや記憶を保存しない。必要な保存は親が実在する保存先へ行う

## 並列 explorer の結果統合

- 複数 explorer のレポートを統合するときは、各レポートの**「対象ファイル + 行範囲」リスト**を正規化して **ファイル単位の和集合** で実装スコープを確定する。特定 explorer の最終提案テーブルだけで実装スコープを決めない
- 同じラベル番号（W-1, W-2 等）が複数 explorer で**違うファイル**を指していることがある。番号で集約せず、ファイル単位で個別に判断する

## explorer 起動プロンプトの字義範囲ガード

- explorer プロンプトに書く調査対象は、**直前会話でユーザーが明示的に扱った具体的トピック**（ファイル名・関数名・機能名・キーワード）にデフォルトで限定する。「網羅的に調べて」「広く確認して」「全体のパターンを」のような広域指示は、本当に網羅が必要なときに限定する
- 直前ターンでユーザーが「これは触らない」「この範囲だけ」と明示したスコープは、explorer プロンプトでも再宣言する。前ターンの限定をスルーした広域探索プロンプトを書かない
- explorer プロンプトを書く前に「ユーザーは直前ターンで具体的に何を扱ったか」を 1〜3 個のキーワードで言語化する。それが思い浮かばないなら、まず人間との対話に戻って範囲を確定する（広域プロンプトを「ひとまず投げる」のは禁止）
- 「最新の動向は？」「全体のパターンを把握して」など字義範囲を超える網羅指示を出す前に、ユーザーが本当にその範囲を望んでいるか自問する。多くの場合、ユーザーは特定のファイル・特定の領域だけを知りたい
