# Foreign project name blocklist for foreign-project-name-guard hook (静的補完用).
#
# 用途: グローバル SSOT (.claude/CLAUDE.md, .claude/agents/**, .claude/skills/**,
#       .claude/hooks/**) への diff に「他プロジェクト固有名」が混入していたら
#       hook が pre-commit で block する。
#
# 動的 vs 静的:
#   - 動的 (foreign-names.cache): ~/.claude/projects/<sanitized>/*.jsonl の cwd
#     フィールドから basename を抽出。Claude Code を起動した全プロジェクトの
#     ディレクトリ名が自動的に対象になる。hook が commit ごとに鮮度判定して再生成。
#   - 静的 (このファイル): 動的では拾えない名前を補完する。具体的にはディレクトリ
#     名と異なるクラス名・ファイル名・組織 slug 等。新しい項目を追加したら次の
#     commit から有効。
#
# 形式: 1 行 1 トークン。空行と # で始まるコメントは無視。マッチは grep -F
#       (literal substring)。

# ===== Class / file names from past incidents =====
QAHelper
WordItemBookDetailPrefab
PopupBookDetail
run_all_reports

# ===== Organization slugs (ディレクトリ名に出ない部分) =====
astran-jp
